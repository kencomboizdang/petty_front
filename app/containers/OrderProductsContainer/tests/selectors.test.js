// import { selectOrderProductsContainerDomain } from '../selectors';

describe('selectOrderProductsContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
